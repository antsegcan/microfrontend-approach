{
  "name": "pocs",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.6",
    "@testing-library/react": "^11.2.2",
    "@testing-library/user-event": "^12.2.2",
    "parcel": "^1.12.4",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-scripts": "4.0.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "serve": "^11.3.2"
  },
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "run-s build:*",
    "build:package": "parcel build --no-content-hash src/react-wrapper.js --no-source-maps",
    "build:server": "serve -l 5000 dist",
    "build:client": "serve -l 5001 client.html"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
